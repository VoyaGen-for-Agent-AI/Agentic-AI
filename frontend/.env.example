# 是否預設使用真實後端 (true) 或模擬資料 (false)。
# demo 現場沒網路時，建議設 false，或直接用畫面上的切換鈕即時切換。
VITE_USE_REAL_BACKEND=false

# FastAPI 後端位址（poetry run uvicorn main:app --reload 預設 8000 埠）
VITE_API_BASE_URL=http://localhost:8000
